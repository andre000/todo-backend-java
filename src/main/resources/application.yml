spring:
  datasource:
    url: jdbc:h2:mem:todo_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update   # dev only
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
